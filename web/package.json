{
  "name": "paddock",
  "version": "1.0.0",
  "private": true,
  "description": "Site F1 — hub week-end, données et pronostics. Le site lui-même n'a aucune dépendance : esbuild ne sert qu'à produire la version de démonstration en un seul fichier.",
  "type": "module",
  "scripts": {
    "demo": "node build-demo.mjs demo.html"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
